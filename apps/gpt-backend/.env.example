# Server
PORT=3000
NODE_ENV=development

# Control Mode (seguridad para acciones)
# - read_only: solo lectura, no escribe nada
# - pr_only: puede crear PRs pero no merge directo
# - full: acceso completo (usar con precaución)
CONTROL_MODE=read_only

# Auth
API_TOKENS=token1,token2,token3

# MongoDB (opcional)
MONGO_URI=

# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=

# Anthropic (Claude)
ANTHROPIC_API_KEY=sk-ant-...

# GitHub
GITHUB_TOKEN=ghp_...

# Vercel (opcional, para deploys)
VERCEL_API_KEY=
VERCEL_TEAM_ID=
VERCEL_PROJECT_ID=
