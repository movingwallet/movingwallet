name: Explain Failure

on:
  workflow_call:
    inputs:
      reason:
        required: false
        type: string
        default: "smoke-backend failed"
      base_url:
        required: false
        type: string
        default: "http://127.0.0.1:3000"

jobs:
  explain:
    runs-on: ubuntu-latest

    steps:
      - name: "Download smoke logs"
        uses: actions/download-artifact@v4
        with:
          name: backend-smoke-logs
          path: logs

      - name: "Explain failure (deterministic first, logs fallback)"
        run: |
          set -e

          BASE_URL="${{ inputs.base_url }}"
          LOG="logs/backend-smoke.log"
          DEBUG_JSON="debug-ai.json"

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "❌ CI FAILURE — DIAGNOSTIC"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

          # 1) Try deterministic diagnostic
          if curl -sSf "$BASE_URL/api/debug/ai" > "$DEBUG_JSON"; then
            echo "🧠 Deterministic diagnostic from /api/debug/ai"
            echo ""

            node - <<'NODE'
            const fs = require("fs");
            const d = JSON.parse(fs.readFileSync("debug-ai.json","utf8"));

            const event = {
              source: "ci",
              kind: "ai_diagnostic",
              provider: d.provider,
              hasKey: d.hasKey,
              keyCheck: d.keyCheck,
              circuit: d?.openai?.circuitBreaker?.state || null,
              model: d?.openai?.model || null,
              baseURL: d?.openai?.baseURL || null,
              timestamp: new Date().toISOString(),
              env: "ci"
            };

            console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
            console.log("📦 Structured diagnostic event");
            console.log("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
            console.log(JSON.stringify(event, null, 2));
            NODE

            exit 0
          fi

          # 2) Fallback to logs
          echo "⚠️ /api/debug/ai not reachable. Falling back to logs."
          echo ""

          if [ ! -f "$LOG" ]; then
            echo "No backend-smoke.log artifact found."
            exit 0
          fi

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🧾 Last 140 lines of backend-smoke.log"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          tail -n 140 "$LOG" || true
